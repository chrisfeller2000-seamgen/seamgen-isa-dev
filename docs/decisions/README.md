# Architecture decisions

No accepted ADRs are recorded here yet. Add one when a choice would otherwise be rediscovered—for example, a future source-of-truth and release policy for the live VM, or an approved non-production integration environment. Do not label the current dev-repo setup as full production parity.

Use a dated file such as `YYYY-MM-DD-short-decision.md`. State context, decision, alternatives, consequences, owner, and verification. Link related [specs](../specs/README.md). Preserve accepted decisions; supersede one with a new linked record instead of rewriting history.
